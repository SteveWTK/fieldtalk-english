The default content surface — flat, hairline-bordered, 12px radius, no shadow.

```jsx
<Card label="Atletas"><div>28</div></Card>
<Card nested label="Prontidão">…</Card>
```

Never add a drop shadow on dark, a coloured left border, or a gradient fill.
